# Repo ini RILIS-ONLY (publik): hanya artefak rilis yang di-track.
# Source code TIDAK PERNAH masuk repo ini (tetap di repo private).
src/
*.o
*.a
*.so
*.log
/tmp/
# secrets lokal (jangan pernah commit)
*.pem
*.key
.env
api.conf
bot.conf
servers.list
*.sqlite3
*.zip
